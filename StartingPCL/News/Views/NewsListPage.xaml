<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="StartingPCL.NewsListPage" Title="{Binding Title}" IsBusy="{Binding IsBusy}"
	xmlns:local="clr-namespace:StartingPCL;assembly=StartingPCL">
	<!--	<ContentPage.Padding>
		<OnPlatform x:TypeArguments="Thickness" iOS="10, 20, 10, 0" Android="10, 0" WinPhone="10, 0" />
	</ContentPage.Padding>-->
	<!--	<ContentPage.Content>
		<StackLayout HorizontalOptions="Center" VerticalOptions="Center">
			<Label Text="News articles..." x:Name="articlesCountLabel" />
		</StackLayout>
	</ContentPage.Content>-->
	<ListView x:Name="listView" ItemsSource="{Binding Articles}" SelectedItem="{Binding SelectedArticle, Mode=TwoWay}">
		<x:Arguments>
			<ListViewCachingStrategy>RecycleElement</ListViewCachingStrategy>
		</x:Arguments>
		<ListView.ItemTemplate>
			<DataTemplate>
				<local:ArticleRowViewCell Text="{Binding Title}" Detail="{Binding Subtitle}" ImageSource="{Binding ListRowImageSource}" />
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>